# Rag_pipeline
This project implements an Automated Retrieval-Augmented Generation (RAG) pipeline that ingests documents, processes them into embeddings, stores them in a vector database, and enables a question-answering interface powered by a Large Language Model. 
